export const metadata = {
    title: "Physical Trade | PrimeLink Exim",
    description: "Source high-quality commodities like Beetroot, Moringa, and more. Authentic verified suppliers.",
};

export default function PhysicalLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
